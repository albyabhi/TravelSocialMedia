import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FAF9F9",
    },
    secondary: {
      main: "#ADD2C9",
    },
    tertiary: {
      main: "#5EA3A3",
    },
    quaternary: {
      main: "#488B8F",
    },
    // You can add more custom color keys as needed
    other: {
      main: 'gray',
    }
  }
});
